<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark window so the launch flash matches the Compose UI's dark theme. -->
    <style name="Theme.CamIP" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@color/camip_background</item>
        <item name="android:windowBackground">@color/camip_background</item>
    </style>

    <!-- Translucent host for the screen-capture consent trampoline: only the
         system's permission dialog should be visible behind it. -->
    <style name="Theme.CamIP.Consent" parent="Theme.CamIP">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>
